// source/core-app.ts

/**
 * This represents the "core application logic"
 * — something that should continue running normally
 * even when faults occur in other components (sensor, etc).
 */
export function coreApplicationLogic() {
  console.log("🧠 Core App: Executing core logic... [e.g., temperature processing, actuator control]");
}
